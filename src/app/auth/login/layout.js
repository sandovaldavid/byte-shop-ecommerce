export const metadata = {
	title: 'Iniciar Sesión | TechStore',
	description:
		'Inicia sesión en TechStore para acceder a los mejores productos tecnológicos',
};

export default function AuthLayout({ children }) {
	return children;
}
